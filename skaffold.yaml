apiVersion: skaffold/v2beta10
kind: Config
metadata:
  name: heliograph-agent
build:
  artifacts:
  - image: heliograph-agent
    sync:
      infer:
        - ".dockerignore"
        - "src/**/*"
        - "*.ts"
        - "*.js*"
        - "yarn.*"
  local:
    useBuildkit: true
deploy:
  kubectl:
    manifests:
    - kubernetes/deployment.yaml
